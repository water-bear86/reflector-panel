## 2026-07-08 - Form and Icon-Only Link Accessibility
**Learning:** Connecting labels to inputs via `htmlFor`/`id` and providing `aria-label` to icon-only links are foundational for screen reader support and hit-area clarity.
**Action:** Always verify that every form input has a corresponding label and every icon-only interactive element has a descriptive `aria-label`.
