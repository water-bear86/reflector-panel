## 2026-07-14 - Dynamic Form Field Accessibility
**Learning:** Generic labels for recurring form elements (like those in a mapped list) fail to provide sufficient context for screen reader users, making it difficult to distinguish between multiple similar inputs.
**Action:** Always use dynamic, context-aware labels (e.g., "Rule 1 percentage") for inputs within loops to ensure each interactive element has a unique and descriptive accessible name.
